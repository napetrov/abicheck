name: 'abicheck — ABI Compatibility Checker'
description: 'Detect ABI/API breaking changes in C/C++ shared libraries during CI.'

branding:
  icon: 'shield'
  color: 'blue'

inputs:
  # ── Operation mode ──────────────────────────────────────────────────────────
  mode:
    description: >
      Operation mode: "compare" (default) compares two ABI surfaces;
      "compare-release" compares all libraries in two directories or packages;
      "dump" generates a JSON baseline snapshot from a single library;
      "appcompat" checks whether an application binary is affected by a library update;
      "deps" resolves transitive dependencies and symbol bindings (Linux ELF);
      "stack-check" compares a binary's full dependency stack across two
      sysroots (Linux ELF).
    required: false
    default: 'compare'

  # ── Library inputs ──────────────────────────────────────────────────────────
  old-library:
    description: >
      Path to the old library, JSON snapshot, or ABICC Perl dump.
      Required when mode=compare. For compare-release: old directory or package
      (RPM, Deb, tar, conda, wheel).
    required: false
  new-library:
    description: >
      Path to the new (current) library or binary.
      Required for compare and dump modes. For compare-release: new directory
      or package (RPM, Deb, tar, conda, wheel).
    required: false

  # ── Application compatibility inputs ────────────────────────────────────
  app-binary:
    description: >
      Path to the application binary (ELF, PE, or Mach-O).
      Required when mode=appcompat.
    required: false
  check-against:
    description: >
      Path to a library for weak-mode symbol availability check (no old library needed).
      Only used with mode=appcompat.
    required: false
  show-irrelevant:
    description: >
      Include library changes that do not affect the application.
      Only used with mode=appcompat.
    required: false
    default: 'false'
  list-required-symbols:
    description: >
      List symbols the application requires from the library and exit.
      Only used with mode=appcompat.
    required: false
    default: 'false'

  # ── Package inputs (compare-release mode) ────────────────────────────────────
  debug-info1:
    description: 'Debug info package for old side (RPM/Deb/tar). compare-release mode only.'
    required: false
  debug-info2:
    description: 'Debug info package for new side (RPM/Deb/tar). compare-release mode only.'
    required: false
  devel-pkg1:
    description: 'Development package with headers for old side. compare-release mode only.'
    required: false
  devel-pkg2:
    description: 'Development package with headers for new side. compare-release mode only.'
    required: false
  dso-only:
    description: 'Only compare shared objects, skip executables. compare-release mode only.'
    required: false
    default: 'false'
  include-private-dso:
    description: 'Include private (non-public) shared objects from non-standard paths. compare-release mode only.'
    required: false
    default: 'false'
  keep-extracted:
    description: 'Keep extracted temporary files for debugging. compare-release mode only.'
    required: false
    default: 'false'
  fail-on-removed-library:
    description: 'Exit 8 when a library present in old is absent in new. compare-release mode only.'
    required: false
    default: 'false'

  # ── Header inputs ───────────────────────────────────────────────────────────
  header:
    description: >
      Public header file(s) applied to both sides (space-separated).
      Required when input is an ELF binary; ignored for JSON snapshots.
    required: false
  old-header:
    description: 'Public header(s) for the old side only (overrides header for old). Space-separated.'
    required: false
  new-header:
    description: 'Public header(s) for the new side only (overrides header for new). Space-separated.'
    required: false

  # ── Include directories ─────────────────────────────────────────────────────
  include:
    description: 'Extra include directories for castxml, applied to both sides. Space-separated.'
    required: false
  old-include:
    description: 'Include directories for old side only. Space-separated.'
    required: false
  new-include:
    description: 'Include directories for new side only. Space-separated.'
    required: false

  # ── Version labels ──────────────────────────────────────────────────────────
  old-version:
    description: 'Version label for the old library (embedded in reports).'
    required: false
    default: 'old'
  new-version:
    description: 'Version label for the new library (embedded in reports).'
    required: false
    default: 'new'

  # ── Language & compiler ─────────────────────────────────────────────────────
  lang:
    description: 'Language mode for castxml: "c++" or "c".'
    required: false
    default: 'c++'
  gcc-path:
    description: 'Path to GCC/G++ cross-compiler binary (dump mode only).'
    required: false
  gcc-prefix:
    description: 'Cross-toolchain prefix, e.g. aarch64-linux-gnu- (dump mode only).'
    required: false
  gcc-options:
    description: 'Extra compiler flags passed through to castxml (dump mode only).'
    required: false
  sysroot:
    description: 'Alternative system root directory for cross-compilation (dump and deps modes).'
    required: false
  nostdinc:
    description: 'Do not search standard system include paths (dump mode only).'
    required: false
    default: 'false'

  # ── Full-stack dependency validation (Linux ELF) ─────────────────────────────
  follow-deps:
    description: >
      Include transitive dependency graph and symbol binding info in dump or
      compare output. ELF only. Applies to dump and compare modes.
    required: false
    default: 'false'
  baseline:
    description: >
      Sysroot for the baseline environment. Required for mode=stack-check.
    required: false
  candidate:
    description: >
      Sysroot for the candidate environment. Required for mode=stack-check.
    required: false
  search-path:
    description: >
      Additional directories to search for shared libraries (space-separated).
      Used by deps, stack-check, and follow-deps modes.
    required: false
  ld-library-path:
    description: >
      Simulated LD_LIBRARY_PATH (colon-separated). Used by deps, stack-check,
      and follow-deps modes.
    required: false

  # ── Output & policy ─────────────────────────────────────────────────────────
  format:
    description: >
      Output format: markdown (default), json, sarif, html.
      sarif and html are only supported in compare mode; other modes
      support markdown and json only (unsupported formats fall back
      to markdown with a warning).
    required: false
    default: 'markdown'
  output-file:
    description: 'Path to write the report file. If not set, output goes to stdout and job summary.'
    required: false
  policy:
    description: 'Built-in policy profile: strict_abi (default), sdk_vendor, plugin_abi.'
    required: false
    default: 'strict_abi'
  policy-file:
    description: 'Path to a YAML policy file with per-kind verdict overrides.'
    required: false
  suppress:
    description: 'Path to a YAML suppression file to filter known/intentional changes.'
    required: false
  verbose:
    description: 'Enable verbose/debug output from abicheck.'
    required: false
    default: 'false'

  # ── Action-specific behaviour ───────────────────────────────────────────────
  python-version:
    description: 'Python version for setup-python.'
    required: false
    default: '3.13'
  install-deps:
    description: 'Install system dependencies (castxml, gcc). Set to false if pre-installed.'
    required: false
    default: 'true'
  upload-sarif:
    description: >
      Upload SARIF results to GitHub Code Scanning (requires security-events: write permission).
      Only effective when format=sarif.
    required: false
    default: 'false'
  fail-on-breaking:
    description: 'Fail the step when a binary ABI break is detected (exit code 4).'
    required: false
    default: 'true'
  fail-on-api-break:
    description: 'Fail the step when a source-level API break is detected (exit code 2).'
    required: false
    default: 'false'
  severity-preset:
    description: >
      Severity preset: 'default', 'strict', or 'info-only'.
      Controls exit codes and report labels. Only applies to compare mode.
    required: false
  severity-addition:
    description: >
      Severity for new public API additions: 'error', 'warning', or 'info'.
      Set to 'error' to fail the step when additions are detected.
      Only applies to compare mode.
    required: false
  extra-args:
    description: 'Additional CLI arguments passed directly to the abicheck command.'
    required: false
    default: ''
  add-job-summary:
    description: 'Write a markdown summary to the GitHub Actions Job Summary. Ignored for dump mode.'
    required: false
    default: 'true'

outputs:
  verdict:
    description: >
      ABI verdict. For compare: COMPATIBLE, SEVERITY_ERROR, API_BREAK,
      BREAKING, or ERROR. SEVERITY_ERROR is produced when
      --severity-addition=error detects new public API additions
      (compare mode only).
      For compare-release: COMPATIBLE, API_BREAK, BREAKING,
      REMOVED_LIBRARY (when fail-on-removed-library is set), or ERROR.
      For appcompat: COMPATIBLE, API_BREAK, BREAKING, or ERROR.
      For dump: COMPATIBLE or ERROR.
      For stack-check: PASS, WARN, FAIL, or ERROR.
      For deps: PASS, FAIL, or ERROR.
    value: ${{ steps.run-abicheck.outputs.verdict }}
  exit-code:
    description: >
      abicheck exit code. compare: 0 (compatible), 1 (severity error),
      2 (API break), 4 (ABI break). compare-release: 0 (compatible),
      2 (API break), 4 (ABI break), 8 (library removed).
      appcompat: 0 (compatible), 2 (API break), 4 (ABI break).
      stack-check: 0 (pass), 1 (warn), 4 (fail).
      deps: 0 (ok), 1 (missing). Click CLI errors are mapped to
      VERDICT=ERROR.
    value: ${{ steps.run-abicheck.outputs.exit-code }}
  report-path:
    description: >
      Path to the generated report file. Set when output-file is provided,
      or auto-populated for SARIF format (defaults to abicheck-results.sarif).
    value: ${{ steps.run-abicheck.outputs.report-path }}

runs:
  using: 'composite'
  steps:
    # ── 1. Python ─────────────────────────────────────────────────────────────
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ inputs.python-version }}

    # ── 2. System dependencies ────────────────────────────────────────────────
    - name: Install system dependencies
      if: inputs.install-deps == 'true'
      shell: bash
      run: bash "${{ github.action_path }}/action/install-deps.sh"

    # ── 3. Install abicheck ──────────────────────────────────────────────────
    - name: Install abicheck
      shell: bash
      run: pip install "${{ github.action_path }}"

    # ── 4. Run abicheck ──────────────────────────────────────────────────────
    - name: Run abicheck
      id: run-abicheck
      shell: bash
      env:
        INPUT_MODE: ${{ inputs.mode }}
        INPUT_OLD_LIBRARY: ${{ inputs.old-library }}
        INPUT_NEW_LIBRARY: ${{ inputs.new-library }}
        INPUT_APP_BINARY: ${{ inputs.app-binary }}
        INPUT_CHECK_AGAINST: ${{ inputs.check-against }}
        INPUT_SHOW_IRRELEVANT: ${{ inputs.show-irrelevant }}
        INPUT_LIST_REQUIRED_SYMBOLS: ${{ inputs.list-required-symbols }}
        INPUT_HEADER: ${{ inputs.header }}
        INPUT_OLD_HEADER: ${{ inputs.old-header }}
        INPUT_NEW_HEADER: ${{ inputs.new-header }}
        INPUT_INCLUDE: ${{ inputs.include }}
        INPUT_OLD_INCLUDE: ${{ inputs.old-include }}
        INPUT_NEW_INCLUDE: ${{ inputs.new-include }}
        INPUT_OLD_VERSION: ${{ inputs.old-version }}
        INPUT_NEW_VERSION: ${{ inputs.new-version }}
        INPUT_LANG: ${{ inputs.lang }}
        INPUT_GCC_PATH: ${{ inputs.gcc-path }}
        INPUT_GCC_PREFIX: ${{ inputs.gcc-prefix }}
        INPUT_GCC_OPTIONS: ${{ inputs.gcc-options }}
        INPUT_SYSROOT: ${{ inputs.sysroot }}
        INPUT_NOSTDINC: ${{ inputs.nostdinc }}
        INPUT_FORMAT: ${{ inputs.format }}
        INPUT_OUTPUT_FILE: ${{ inputs.output-file }}
        INPUT_POLICY: ${{ inputs.policy }}
        INPUT_POLICY_FILE: ${{ inputs.policy-file }}
        INPUT_SUPPRESS: ${{ inputs.suppress }}
        INPUT_VERBOSE: ${{ inputs.verbose }}
        INPUT_FOLLOW_DEPS: ${{ inputs.follow-deps }}
        INPUT_BASELINE: ${{ inputs.baseline }}
        INPUT_CANDIDATE: ${{ inputs.candidate }}
        INPUT_SEARCH_PATH: ${{ inputs.search-path }}
        INPUT_LD_LIBRARY_PATH: ${{ inputs.ld-library-path }}
        INPUT_FAIL_ON_BREAKING: ${{ inputs.fail-on-breaking }}
        INPUT_FAIL_ON_API_BREAK: ${{ inputs.fail-on-api-break }}
        INPUT_SEVERITY_PRESET: ${{ inputs.severity-preset }}
        INPUT_SEVERITY_ADDITION: ${{ inputs.severity-addition }}
        INPUT_EXTRA_ARGS: ${{ inputs.extra-args }}
        INPUT_ADD_JOB_SUMMARY: ${{ inputs.add-job-summary }}
        INPUT_DEBUG_INFO1: ${{ inputs.debug-info1 }}
        INPUT_DEBUG_INFO2: ${{ inputs.debug-info2 }}
        INPUT_DEVEL_PKG1: ${{ inputs.devel-pkg1 }}
        INPUT_DEVEL_PKG2: ${{ inputs.devel-pkg2 }}
        INPUT_DSO_ONLY: ${{ inputs.dso-only }}
        INPUT_INCLUDE_PRIVATE_DSO: ${{ inputs.include-private-dso }}
        INPUT_KEEP_EXTRACTED: ${{ inputs.keep-extracted }}
        INPUT_FAIL_ON_REMOVED_LIBRARY: ${{ inputs.fail-on-removed-library }}
      run: bash "${{ github.action_path }}/action/run.sh"

    # ── 5. Upload SARIF ──────────────────────────────────────────────────────
    - name: Upload SARIF to Code Scanning
      if: inputs.upload-sarif == 'true' && inputs.format == 'sarif' && steps.run-abicheck.outputs.report-path != ''
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: ${{ steps.run-abicheck.outputs.report-path }}
        category: abicheck
      continue-on-error: true
